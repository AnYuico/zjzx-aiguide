<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tzp.zjzx.user.mapper.UserInfoMapper">
    <resultMap id="userInfoMap" type="com.tzp.zjzx.model.entity.user.UserInfo" autoMapping="true"/>

    <!-- 用于select查询公用抽取的列 -->
    <sql id="columns">
        id,username,password,nick_name,avatar,sex,phone,memo,open_id,union_id,last_login_ip,last_login_time,status,create_time,update_time,is_deleted
    </sql>

    <!-- 1.根据用户名查询用户是否存在-->
    <select id="selectByUserName" resultMap="userInfoMap">
        select <include refid="columns" />
        from user_info
        where
        username = #{username}
        and is_deleted = 0
    </select>

    <select id="selectById" resultMap="userInfoMap">
        select <include refid="columns" />
        from user_info
        where id = #{userId}
        and is_deleted = 0
    </select>

    <!-- 2.添加用户-->
    <insert id="save">
        insert into user_info (
            id,
            username,
            password,
            nick_name,
            avatar,
            sex,
            phone,
            memo,
            open_id,
            union_id,
            last_login_ip,
            last_login_time,
            status
        ) values (
                     #{id},
                     #{username},
                     #{password},
                     #{nickName},
                     #{avatar},
                     #{sex},
                     #{phone},
                     #{memo},
                     #{openId},
                     #{unionId},
                     #{lastLoginIp},
                     #{lastLoginTime},
                     #{status}
                 )
    </insert>

    <update id="upgradePassword">
        update user_info
        set password = #{newPassword},
            update_time = now()
        where id = #{userId}
          and password = #{oldPassword}
          and is_deleted = 0
    </update>

    <update id="resetTestUser">
        update user_info
        set password = #{password},
            nick_name = #{nickName},
            phone = #{phone},
            avatar = #{avatar},
            sex = 0,
            status = 1,
            update_time = now()
        where id = #{userId}
          and memo = #{marker}
          and is_deleted = 0
    </update>
</mapper>

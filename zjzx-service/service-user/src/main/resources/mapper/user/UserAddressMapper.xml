<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tzp.zjzx.user.mapper.UserAddressMapper">

    <resultMap id="userAddressMap" type="com.tzp.zjzx.model.entity.user.UserAddress" autoMapping="true"/>

    <!-- 用于select查询公用抽取的列 -->
    <sql id="columns">
        id,user_id,name,phone,tag_name,province_code,city_code,district_code,address,full_address,is_default,create_time,update_time,is_deleted
    </sql>

    <!-- 获取用户地址列表-->
    <select id="findUserAddressList" resultMap="userAddressMap">
        select <include refid="columns" />
        from user_address
        where user_id = #{userId}
          and is_deleted = 0
        order by is_default desc, id desc
    </select>

    <!-- 根据id获取收货地址信息-->
    <select id="getUserAddress" resultMap="userAddressMap">
        select <include refid="columns" />
        from user_address
        where id = #{id}
          and user_id = #{userId}
          and is_deleted = 0
    </select>

    <select id="lockUser" resultType="java.lang.Long">
        select id
        from user_info
        where id = #{userId}
          and is_deleted = 0
        for update
    </select>

    <select id="countActive" resultType="int">
        select count(1)
        from user_address
        where user_id = #{userId}
          and is_deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into user_address (
            user_id,name,phone,tag_name,province_code,city_code,district_code,
            address,full_address,is_default,create_time,update_time,is_deleted
        ) values (
            #{userId},#{name},#{phone},#{tagName},#{provinceCode},#{cityCode},#{districtCode},
            #{address},#{fullAddress},#{isDefault},now(),now(),0
        )
    </insert>

    <update id="updateOwned">
        update user_address
        set name = #{name},
            phone = #{phone},
            tag_name = #{tagName},
            province_code = #{provinceCode},
            city_code = #{cityCode},
            district_code = #{districtCode},
            address = #{address},
            full_address = #{fullAddress},
            is_default = #{isDefault},
            update_time = now()
        where id = #{id}
          and user_id = #{userId}
          and is_deleted = 0
    </update>

    <update id="clearDefault">
        update user_address
        set is_default = 0,
            update_time = now()
        where user_id = #{userId}
          and is_default = 1
          and is_deleted = 0
    </update>

    <update id="logicalDelete">
        update user_address
        set is_deleted = 1,
            is_default = 0,
            update_time = now()
        where id = #{id}
          and user_id = #{userId}
          and is_deleted = 0
    </update>

    <select id="findFirstAddressId" resultType="java.lang.Long">
        select id
        from user_address
        where user_id = #{userId}
          and is_deleted = 0
        <if test="excludeId != null">
            and id != #{excludeId}
        </if>
        order by id desc
        limit 1
    </select>

    <update id="setDefault">
        update user_address
        set is_default = 1,
            update_time = now()
        where id = #{id}
          and user_id = #{userId}
          and is_deleted = 0
    </update>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tzp.zjzx.order.mapper.PaymentExceptionTaskMapper">

    <insert id="insertIgnore">
        insert ignore into payment_exception_task(
            event_id, order_no, trade_no, amount, reason, status
        ) values (
            #{event.eventId}, #{event.orderNo}, #{event.tradeNo},
            #{event.amount}, #{reason}, 0
        )
    </insert>
</mapper>

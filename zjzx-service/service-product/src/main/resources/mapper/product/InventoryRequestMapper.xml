<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tzp.zjzx.product.mapper.InventoryRequestMapper">

    <resultMap id="inventoryRequestMap" type="com.tzp.zjzx.model.entity.product.InventoryRequest" autoMapping="true"/>

    <insert id="insertIgnore" useGeneratedKeys="true" keyProperty="id">
        insert ignore into inventory_request(order_no, items_hash, status)
        values (#{orderNo}, #{itemsHash}, #{status})
    </insert>

    <select id="selectByOrderNoForUpdate" resultMap="inventoryRequestMap">
        select id, order_no, items_hash, status, create_time, update_time, is_deleted
        from inventory_request
        where order_no = #{orderNo} and is_deleted = 0
        for update
    </select>

    <update id="updateStatus">
        update inventory_request
        set status = #{targetStatus}, update_time = now()
        where order_no = #{orderNo}
          and status = #{expectedStatus}
          and is_deleted = 0
    </update>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tzp.zjzx.manager.mapper.OrderStatisticsMapper">


    <!-- 用于select查询公用抽取的列 -->
    <sql id="columns">
        id,order_date,total_amount,total_num,create_time,update_time,is_deleted
    </sql>

    <insert id="incrementPaidOrder">
        insert into order_statistics(order_date, total_amount, total_num)
        values (#{orderDate}, #{amount}, 1)
        on duplicate key update
            total_amount = total_amount + values(total_amount),
            total_num = total_num + 1,
            update_time = now(),
            is_deleted = 0
    </insert>

    <insert id="upsertSnapshot">
        insert into order_statistics(order_date, total_amount, total_num)
        values (#{orderDate}, #{totalAmount}, #{totalNum})
        on duplicate key update
            total_amount = values(total_amount),
            total_num = values(total_num),
            update_time = now(),
            is_deleted = 0
    </insert>

    <!-- 查询结果表中的统计数据-->
    <select id="selectList" resultType="com.tzp.zjzx.model.entity.order.OrderStatistics">
        select <include refid="columns" />
        from order_statistics
        <where>
            <if test="createTimeBegin != null and createTimeBegin != ''">
                and order_date >= #{createTimeBegin}
            </if>
            <if test="createTimeEnd != null and createTimeEnd != ''">
                and order_date &lt;= #{createTimeEnd}
            </if>
        </where>
        order by order_date
    </select>
</mapper>

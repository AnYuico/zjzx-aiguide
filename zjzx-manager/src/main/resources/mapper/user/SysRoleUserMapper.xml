<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tzp.zjzx.manager.mapper.SysRoleUserMapper">

    
    <!-- 1.根据用户id删除角色数据-->
    <delete id="deleteByUserId">
        delete FROM sys_user_role where user_id=#{userId}
    </delete>

    <!-- 2.为用户重新分配角色-->
    <insert id="doAssign">
        insert into sys_user_role (user_id , role_id , create_time , update_time , is_deleted)
        values (#{userId} , #{roleId} , now() , now() , 0);
    </insert>

    <!-- 3.根据用户id查询用户被分配过的角色列表-->
    <select id="selectRoleIdsByUserId" resultType="Long">
        select role_id from sys_user_role where user_id=#{userId}
    </select>

    <select id="selectUserIdsByRoleId" resultType="Long">
        select distinct user_id
        from sys_user_role
        where role_id=#{roleId}
        and is_deleted=0
    </select>
</mapper>
